import os

Import('env')

env['LIBS'] += ['vulkan']

if os.uname()[0] == 'Linux':
    env.ParseConfig('pkg-config --cflags --libs x11-xcb')

objects = map(env.Object, [
    'command_buffer.cc',
    'device.cc',
    'loader.cc',
    'queue.cc',
    'stubs.cc',
    'surface.cc',
    'swapchain.cc',
    'vulkan.cc',
])

Return('objects')
