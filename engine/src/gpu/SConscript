sources = SConscript(dirs = [
    'gl',
])

sources += map(File, [
    'buffer.cc',
    'gpu.cc',
    'index_data.cc',
    'pipeline.cc',
    'vertex_data.cc',
    'vertex_format.cc',
])

Return('sources')
