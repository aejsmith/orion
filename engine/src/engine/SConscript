sources = map(File, [
    'asset.cc',
    'asset_loader.cc',
    'asset_manager.cc',
    'engine.cc',
    'main.cc',
    'render_target.cc',
    'texture.cc',
    'window.cc',
])

sources += SConscript(dirs = [
    'loaders',
])

Return('sources')
