import os

sources = map(File, [
    'engine_global.cc',
    'error.cc',
    'filesystem.cc',
    'log.cc',
    'path.cc',
    'refcounted.cc',
    'utility.cc',
])

sources += SConscript(dirs = [
    'math',
])

sources += SConscript(dirs = [os.name])

Return('sources')
